# Baseline de Fase 0 — 2026-08-12

Este registro acompaña el plan de [`RGB Dog × WLED`](../analisis-wled-y-plan-implementacion.md). Separa evidencia ejecutada, automatización preparada y validación física todavía pendiente.

## Identidad de la línea base

| Campo | Valor |
|---|---|
| Commit base | `eb8ab3e622a142422baeaaf32bf8cb5fcb4a0e30` (`main`) |
| Proyecto PlatformIO | `Platformio/Dog-RGB` |
| Entorno de producción | `seeed_xiao_esp32s3` |
| PlatformIO Core fijado en CI | `6.1.19` |
| Plataforma ESP32 | `pioarduino 55.03.311` |
| Arduino-ESP32 / ESP-IDF | `3.3.11` / `5.5.5` |
| ArduinoJson | `7.4.3` |
| Adafruit NeoPixel | `1.15.5` |

La Fase 0 no cambia el comportamiento del firmware de producción. Los cambios de código ejecutable empiezan en la Fase 1.

## Evidencia de software

### Suite host

Comando, ejecutado desde `Platformio/Dog-RGB`:

```powershell
python -m unittest discover -s test -p "test_*.py" -v
```

Resultado local:

```text
114 pruebas ejecutadas
114 correctas
0 fallos
```

Se corrigieron dos expectativas obsoletas de contratos fuente:

- la prueba de configuración ahora valida individualmente las tres rutas que mutan `RuntimeConfig`, en vez de depender de un conteo global frágil;
- la prueba de credenciales Wi-Fi espera el envelope uniforme del portal: `{"status":"error","reason":"storage"}`.

El firmware no se modificó para satisfacer estas pruebas; las expectativas se alinearon con el contrato vigente y consistente.

### Portal local

Resultados locales:

| Verificación | Resultado |
|---|---|
| `python tools/web_pages_smoke.py` | Correcto |
| `npx playwright test --project=iphone-13-pro-max-chromium` | 72/72 correctas |
| Comparación visual Linux | Pendiente del job con renderer/container fijado |

El run de Playwright incluye comportamiento, accesibilidad, estados visuales generados y auditorías del portal. La comparación píxel a píxel autoritativa permanece en CI porque los baselines fueron creados con el contenedor Linux fijado.

### Wokwi

Los tests host de assets, diagrama, escenarios y analizador Wokwi están incluidos en las 114 pruebas verdes. La suite que ejecuta el firmware en el servicio Wokwi no se corrió localmente porque no hay `WOKWI_CLI_TOKEN` ni `.env` en este entorno. Debe ejecutarse antes de declarar validado un cambio posterior de comportamiento del firmware.

### CI

`.github/workflows/ci.yml` incorpora:

- job independiente `Firmware (host tests)`;
- PlatformIO Core fijado a `6.1.19`;
- build de producción con plataforma/framework/librerías ya fijados en `platformio.ini`;
- reporte `pio run -t size` en el summary del job;
- inventario de entorno y paquetes;
- hashes SHA-256 y tamaños de BIN/ELF/MAP disponibles;
- artefacto descargable por 14 días.

Los porcentajes de RAM y flash se registrarán desde un build exitoso; no se escriben valores estimados en este baseline.

El intento local de preparar el toolchain no se usa como evidencia: la primera extracción superó la ventana de ejecución y luego apareció otro build externo usando el mismo almacén global de PlatformIO. El segundo intento propio se detuvo para no competir con ese proceso. El job Linux limpio de CI queda como fuente autoritativa.

### Orden de arranque

[`architecture.md`](../architecture.md) refleja el orden real comprobado en `src/main.cpp`:

```text
storage → config → GPS → geofence → LEDs → BLE opcional → margen de coexistencia → Wi-Fi → portal → Wokwi
```

BLE se inicializa antes de Wi-Fi deliberadamente para no introducir el controlador Bluetooth después de levantar SoftAP. BLE permanece deshabilitado por defecto.

### Política de procedencia/licencia

[`ADR-0001`](../adr/0001-wled-clean-room-y-licencia-del-proyecto.md) establece implementación clean-room de ideas de WLED y prohíbe reutilización literal hasta que el propietario seleccione la licencia de RGB Dog y se revise la procedencia.

## Baseline de RAM y flash

La fuente autoritativa será el artefacto `firmware-baseline-seeed-xiao-esp32s3-<sha>` del job `Firmware (PlatformIO)`.

| Métrica | Valor | Fuente |
|---|---:|---|
| RAM estática usada | Pendiente de build exitoso | `size.txt` del artefacto CI |
| RAM disponible | Pendiente de build exitoso | `size.txt` del artefacto CI |
| Flash usada | Pendiente de build exitoso | `size.txt` del artefacto CI |
| Partición de aplicación | `0x330000` por slot | `partitions_dog_rgb.csv` |
| `firmware.bin` | Pendiente de build exitoso | `files.txt` + SHA-256 |
| `firmware.elf` | Pendiente de build exitoso | `files.txt` + SHA-256 |

Después del primer CI verde se deben copiar aquí los valores del artifact o enlazar el run estable. Esa acción no debe sustituirse con cifras de otra placa, otro environment o un build Wokwi.

## Baseline eléctrico

### Estado

**No medido todavía.** El repositorio no identifica aún los modelos definitivos de celda, boost, BMS/cargador, regulador, conectores o cableado, y este entorno no tiene acceso al collar ni a instrumentos. Cualquier cifra presentada como “medida” sería falsa.

Datos conocidos que son de diseño, no mediciones:

- 48 píxeles SK6812 RGBW por defecto;
- brillo inicial `77/255`;
- no existe un límite de corriente por frame;
- techo conservador de planeación: 2,88–3,84 A a 5 V para 48 píxeles a 60–80 mA/píxel;
- consumo MCU/GNSS/radio y pérdidas de conversión son adicionales.

### Bloqueadores antes de energizar el peor caso

- Registrar número de parte y rating continuo/pico de la celda.
- Registrar límites del BMS/protección y comportamiento de corte.
- Registrar rating real y curva térmica/eficiencia del boost.
- Verificar calibre de cables, conectores, interruptor, fusible y retorno de tierra.
- Definir temperatura máxima aceptable para cada punto y criterio de aborto.
- Confirmar que el collar no estará puesto sobre el animal durante ninguna prueba de banco.

No se debe puentear el BMS, cortocircuitar la celda ni usar la protección como limitador normal de corriente.

### Instrumentación mínima

- Fuente/celda protegida y conocida.
- Medición de corriente y tensión en entrada de celda y rail de 5 V.
- Osciloscopio o captura de mínimo de rail para transitorios, si está disponible.
- Termopares o instrumento de temperatura adecuado para celda, boost, conectores, MCU y tiras.
- Cronómetro/logger y registro de temperatura ambiente.

### Matriz a completar en hardware

Todos los valores deben incluir firmware commit, revisión de hardware, instrumento, estado de carga, temperatura ambiente y duración.

| Perfil | Brillo/escena | Radio/GNSS | I celda prom./pico | I 5 V prom./pico | 5 V mín. | 3,3 V mín. | T máximas | Resultado |
|---|---|---|---:|---:|---:|---:|---|---|
| Equipo apagado | N/A | N/A | — | — | — | — | — | Pendiente |
| Boot/adquisición | Default | AP + GNSS buscando | — | — | — | — | — | Pendiente |
| Estado quieto | Cuerpo apagado | AP | — | — | — | — | — | Pendiente |
| Paseo típico | Perfil declarado | GNSS confiable | — | — | — | — | — | Pendiente |
| Show | Default | AP con tráfico | — | — | — | — | — | Pendiente |
| Blanco controlado | 10 %, 30 %, máximo permitido | Peor radio previsto | — | — | — | — | — | Pendiente |
| Day Mode | Cuerpo apagado | Igual al perfil típico | — | — | — | — | — | Pendiente |

### Secuencia de prueba

1. Inspección visual, continuidad/polaridad y revisión de ratings sin energía.
2. Arranque con LEDs de cuerpo apagados y límite de corriente externo conservador.
3. Boot/adquisición y observación de resets/caídas de rail.
4. Perfiles normales a brillo default hasta estabilidad térmica.
5. Escalamiento breve y vigilado de blanco controlado: 10 %, luego 30 % y solo después el máximo físicamente autorizado.
6. Prueba térmica sostenida del peor patrón **permitido**, no del máximo teórico si el hardware no lo soporta.
7. Runtime con la celda real y un perfil reproducible.
8. Archivo de resultados y fotos del montaje/instrumentación.

Ante olor, hinchamiento, calentamiento inesperado, caída rápida de tensión, inestabilidad o rating excedido, cortar la prueba y revisar el diseño.

La hoja de cálculo conceptual y criterios completos están en [`bom_power_budget.md`](../bom_power_budget.md).

## Estado de salida de Fase 0

| Entregable | Estado |
|---|---|
| Contratos host alineados | Completo |
| Suite host verde | Completo: 114/114 |
| Smoke del portal | Completo |
| Playwright local | Completo: 72/72 |
| Comparación visual Linux | Pendiente observar CI |
| Wokwi runtime | Pendiente token/servicio; contratos host verdes |
| Suite host como gate de CI | Completo en workflow; pendiente observar run remoto |
| Captura reproducible de RAM/flash | Implementada; pendiente primer build exitoso/CI |
| Orden BLE/Wi-Fi documentado | Completo |
| Política provisional de licencia/procedencia | Completo |
| Presupuesto eléctrico provisional | Documentado, no medido |
| Mediciones físicas | Pendiente hardware, BOM congelada e instrumentos |

La Fase 1 no debe fijar un presupuesto de corriente definitivo hasta completar las mediciones físicas. Sí puede construir el estimador y sus pruebas usando un default conservador y claramente provisional.
