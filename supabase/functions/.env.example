# Copy this file to the ignored supabase/functions/.env only for development.
# Replace both values with independent cryptographically random values.
CLAIM_HMAC_PEPPER=replace-me-with-an-independent-random-value
DEVICE_CREDENTIAL_PEPPER=replace-me-with-a-different-random-value
