C++ Arduino style inferred from code: two-space indentation; braces on same line; snake_case functions/locals and UPPER_SNAKE_CASE constants; domain namespaces with anonymous namespaces for private state; headers use #pragma once except legacy config.h/pins.h guards; fixed-width integer types for persisted/on-wire structs; static_assert sizes and packed attributes for binary records; nonblocking millis-difference scheduling is preferred, with a few boot-only delay calls; public APIs declared in include/<domain>; Serial logs use bracketed subsystem tags. No formatter/linter configuration is present. Preserve module encapsulation and update docs when firmware/hardware behavior changes.