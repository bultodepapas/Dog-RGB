For firmware changes: inspect `git diff`, run `git diff --check`, run `python -m unittest test.test_day_mode_static -v`, and build environment `seeed_xiao_esp32s3` with PlatformIO. Add focused native/device tests for changed logic where possible. For portal page changes: extract/serve pages and run the Playwright visual suite. For hardware/power changes: update hardware/README.md and relevant construction/wiring/power docs. Report any check that could not run (for example missing PlatformIO) separately from code failures. Do not treat the current static substring tests as behavioral coverage.